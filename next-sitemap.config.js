/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: process.env.SITE_URL || 'https://abesat.es', // Reemplaza con la URL de tu sitio
  generateRobotsTxt: true, // (Opcional) Genera un archivo robots.txt
  // Otras opciones de configuración según tus necesidades
};
